/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package xyz.dsemikin.yuts.app;

import xyz.dsemikin.yuts.list.LinkedList;

import static xyz.dsemikin.yuts.utilities.StringUtils.join;
import static xyz.dsemikin.yuts.utilities.StringUtils.split;
import static xyz.dsemikin.yuts.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
